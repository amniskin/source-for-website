(ns content.home.top
  (:require [ui.style :as style]))

(defelem content [_ _]
  (style/jumbotron
    (style/container
      (div :class "col-lg-12 col-md-12 col-sm-12"
        (h1 "Tutoring")
        (p "Need a tagline...")
        ))))
